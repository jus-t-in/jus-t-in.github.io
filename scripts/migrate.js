import fs from 'fs';
import path from 'path';

// read posts directly by compiling it or just read the raw file since it's an object. 
// Actually I can just write a script that regexes the posts array, but wait...
